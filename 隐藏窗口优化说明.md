# NoVNC Client 隐藏窗口功能说明

## 功能概述

本版本新增了**窗口隐藏功能**，优化多窗口场景下的资源占用，同时保持GPU渲染和大漠DX抓图功能正常工作。

## 主要特性

1. **默认显示模式** - 软件启动时所有窗口正常显示，便于用户确认功能正常
2. **一键隐藏/显示** - 通过右下角控制栏的"隐藏"按钮一键切换
3. **保持GPU渲染** - 隐藏窗口后仍在GPU上持续渲染，不影响大漠DX抓图
4. **大漠兼容** - 完全兼容易语言大漠调用，窗口句柄保持有效

## 使用方法

### 1. 启动软件
```bash
novnc-cef-client.exe
```

软件启动后，默认会显示所有VNC窗口和右下角控制栏。

### 2. 隐藏窗口
点击右下角控制栏的 **"隐藏"** 按钮：
- 所有VNC窗口立即隐藏
- 窗口句柄保持有效
- GPU渲染继续进行
- 按钮文字变为 "显示"（黄色）

### 3. 显示窗口
再次点击 **"显示"** 按钮：
- 所有VNC窗口重新显示
- 保持之前的渲染状态
- 按钮文字变为 "隐藏"（蓝色）

## 控制栏说明

右下角控制栏包含三个按钮：

| 按钮 | 颜色 | 功能 | 状态变化 |
|------|------|------|----------|
| 控制 | 绿色 | 开启/关闭控制模式 | 绿色→红色 |
| 隐藏 | 蓝色 | 隐藏/显示所有窗口 | 蓝色→黄色 |
| 退出 | 红色 | 退出程序 | 无 |

### 按钮状态示例

**初始状态：**
```
[控制] [隐藏] [退出]
  绿色   蓝色   红色
```

**隐藏模式激活：**
```
[控制] [显示] [退出]
  绿色   黄色   红色
```

## 技术细节

### 隐藏窗口 vs 完全销毁

| 特性 | 隐藏窗口 | 完全销毁 |
|------|----------|----------|
| 窗口句柄 (HWND) | ✅ 有效 | ❌ 无效 |
| GPU渲染 | ✅ 持续渲染 | ❌ 停止渲染 |
| 大漠抓图 | ✅ 正常工作 | ❌ 无法抓图 |
| 资源占用 | ⚠️ 中等（无合成开销） | ✅ 最低 |
| 恢复速度 | ✅ 即时恢复 | ❌ 需重新创建 |

### 渲染优化

隐藏模式下，以下优化生效：

1. **GPU加速保持** - `hardwareAcceleration: true`
2. **避免背景节流** - `disable-backgrounding-occluded-windows`
3. **无窗口合成** - 不需要将画面合成到桌面
4. **渲染循环持续** - CEF继续渲染到窗口DC

### 大漠DX抓图验证

```易语言
.局部变量 hwnd, 整数型
.局部变量 img, 字节集

' 查找窗口（隐藏模式下仍可找到）
hwnd = dm.FindWindow("Chrome_WidgetWin_1", "")
.如果真 (hwnd > 0)
    调试输出 ("窗口句柄：" + 到文本 (hwnd))

    ' DX抓图
    img = dm.CaptureGIF(0, 0, 200, 200, "test.gif")
    .如果真结束
```

## 命令行参数

### 启动时隐藏窗口（推荐用于自动化场景）

```bash
# 启动时直接隐藏所有窗口，GPU渲染继续
novnc-cef-client.exe --hidden=1

# 启动时显示所有窗口（默认）
novnc-cef-client.exe --hidden=0
```

**隐藏启动模式特性：**
- 窗口从创建时就隐藏，GPU渲染持续运行
- Log.txt 中输出每个窗口的标题和句柄（HWND，10进制）
- 完美支持大漠DX抓图，无需额外配置
- 控制栏始终可见，可随时切换显示/隐藏

**Log.txt 句柄输出示例：**
```
[2026/05/13 10:15:23] 启动模式: 隐藏窗口 (GPU渲染继续，窗口可由大漠抓图)
[2026/05/13 10:15:25] 窗口 1: 标题="窗口1" HWND=332340
[2026/05/13 10:15:27] 窗口 2: 标题="窗口2" HWND=986480
[2026/05/13 10:15:29] 窗口 3: 标题="窗口3" HWND=11259378
```

### 延迟创建窗口（减少资源压力）

```bash
# 间隔1.5秒创建每个窗口
novnc-cef-client.exe --delay=1500

# 同时创建所有窗口（默认）
novnc-cef-client.exe --delay=0
```

### 组合使用参数

```bash
# 隐藏启动 + 延迟创建（推荐用于自动化）
novnc-cef-client.exe --hidden=1 --delay=1500

# 指定配置文件 + 隐藏启动
novnc-cef-client.exe --config=自定义配置.int --hidden=1
```

## API接口

原有的HTTP API接口保持不变，隐藏模式不影响以下接口：

- `/status` - 查看状态
- `/set-master` - 设置主控窗口
- `/refresh` - 刷新窗口画面
- `/sync` - 同步事件
- POST `/` - 外部控制命令

## 使用场景

### 推荐使用流程

1. **启动确认** - 正常启动，确认所有窗口连接正常
2. **点击隐藏** - 点击右下角"隐藏"按钮，释放桌面空间
3. **大漠抓图** - 使用大漠DX抓图功能正常工作
4. **需要查看** - 点击"显示"按钮恢复窗口显示

### 典型应用场景

- **多窗口群控** - 15个窗口同时运行，隐藏后减少视觉干扰
- **自动化脚本** - 程序后台运行，通过大漠抓图监控状态
- **资源优化** - 减少窗口合成开销，提升系统性能

## 日志输出

隐藏模式切换时会在 `Log.txt` 中记录：

```
[2026/05/13 09:30:15] 隐藏模式 ON (所有窗口已隐藏，GPU渲染继续)，影响 5 个窗口
[2026/05/13 09:35:20] 隐藏模式 OFF (所有窗口已显示)，影响 5 个窗口
```

## 常见问题

### Q: 隐藏后大漠还能抓图吗？
A: 可以！窗口虽然不可见，但GPU渲染仍在继续，大漠DX抓图正常工作。

### Q: 隐藏后程序会卡顿吗？
A: 不会。隐藏模式减少了窗口合成开销，反而可能提升性能。

### Q: 如何确认窗口真的存在？
A: 使用PowerShell命令检查：
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*electron*"}
```

### Q: 隐藏模式会影响控制功能吗？
A: 不会。控制模式与隐藏模式独立工作，互不影响。

### Q: 隐藏后可以单独显示某个窗口吗？
A: 当前版本只支持全部隐藏/显示，如需单独控制，请使用API接口自定义。

## 更新日志

### v1.1.0 (2026-05-13)
- ✨ 新增隐藏窗口功能
- ✨ 右下角控制栏增加"隐藏/显示"按钮
- 🐛 修复多窗口资源占用过高问题
- 📝 更新使用文档

## 技术支持

如有问题，请在GitHub提交Issue：
https://github.com/hogan-hong/novnc-cef-client/issues

---

**注意**：隐藏窗口功能不影响现有的所有功能，可以安全使用。