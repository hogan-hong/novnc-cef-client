# 隐藏窗口优化说明

## 功能描述

该优化版本支持窗口隐藏模式，在保持GPU渲染和大漠DX抓图功能的同时，减少系统资源占用。

## 主要特性

### 1. 窗口隐藏模式
- 窗口初始可以隐藏（`--hidden=1`）
- 保持GPU加速和渲染上下文
- 支持大漠DX抓图
- 画面持续在GPU刷新
- 支持运行时动态切换显示/隐藏

### 2. 资源优化
- 隐藏窗口时不销毁渲染上下文
- 减少可见窗口的GPU渲染压力
- 保持窗口句柄（HWND）有效，便于大漠调用

### 3. 兼容性
- 完全兼容现有的易语言大漠调用
- 支持所有现有的API接口
- 不影响VNC连接和控制功能

## 使用方法

### 启动方式

#### 1. 隐藏窗口模式（推荐用于大漠抓图）
```bash
novnc-cef-client.exe --hidden=1
```

#### 2. 正常显示模式
```bash
novnc-cef-client.exe --hidden=0
# 或直接运行（默认显示）
novnc-cef-client.exe
```

#### 3. 其他参数组合
```bash
# 隐藏窗口 + 创建延迟（减少资源压力）
novnc-cef-client.exe --hidden=1 --delay=1500

# 隐藏窗口 + 自定义配置文件
novnc-cef-client.exe --hidden=1 --config=自定义配置.int
```

### API 接口

#### 显示/隐藏窗口

**请求格式：**
```http
POST /visibility
Content-Type: application/json

{
  "windowIndex": 1,    // 1-based，0表示所有窗口
  "show": true/false   // true=显示，false=隐藏
}
```

**示例：**
```bash
# 显示所有窗口
curl -X POST http://127.0.0.1:38981/visibility -H "Content-Type: application/json" -d "{"windowIndex":0,"show":true}"

# 隐藏窗口1
curl -X POST http://127.0.0.1:38981/visibility -H "Content-Type: application/json" -d "{"windowIndex":1,"show":false}"

# 隐藏所有窗口
curl -X POST http://127.0.0.1:38981/visibility -H "Content-Type: application/json" -d "{"windowIndex":0,"show":false}"
```

**响应格式：**
```json
{
  "ok": true,
  "show": false,
  "affected": 5
}
```

### 易语言调用示例

#### 调用示例1：隐藏窗口进行抓图
```易语言
.版本 2

.程序集 窗口程序集_启动窗口

.子程序 _启动窗口_创建完毕
    .局部变量 大漠对象, 对象
    .局部变量 返回值, 文本型

    ' 创建大漠对象
    大漠对象.创建 ("dm.dmsoft", )

    ' 启动NoVNC客户端（隐藏窗口）
    运行程序 ("novnc-cef-client.exe --hidden=1", , 假)

    ' 等待VNC连接建立
    延时 (3000)

    ' 获取窗口句柄（大漠可以直接通过标题搜索）
    返回值 ＝ 大漠对象.查找窗口 ("", "窗口1|控制IP地址")

    ' 如果需要隐藏特定窗口
    运行程序 ("curl -X POST http://127.0.0.1:38981/visibility -H "Content-Type: application/json" -d "{"windowIndex":1,"show":false}"", , 真)

    ' 正常进行抓图操作
    返回值 ＝ 大漠对象.抓图 (返回值, "screenshot.bmp")

    ' 处理完成后显示窗口
    运行程序 ("curl -X POST http://127.0.0.1:38981/visibility -H "Content-Type: application/json" -d "{"windowIndex":0,"show":true}"", , 真)
```

#### 调用示例2：动态切换窗口状态
```易语言
.子程序 切换窗口状态, , 公开
    .参数 是否显示, 逻辑型
    .参数 窗口索引, 整数型, 可空

    .局部变量 命令文本, 文本型

    ' 构建JSON请求
    .如果 (窗口索引 ＝ 0)
        命令文本 ＝ "{"windowIndex":0,"show":" ＋ 到文本 (是否显示) ＋ "}"
    .否则
        命令文本 ＝ "{"windowIndex":" ＋ 到文本 (窗口索引) ＋ ","show":" ＋ 到文本 (是否显示) ＋ "}"
    .如果结束

    ' 发送HTTP请求
    运行程序 ("curl -X POST http://127.0.0.1:38981/visibility -H "Content-Type: application/json" -d "" ＋ 命令文本 ＋ """, , 真)
```

## 技术原理

### 1. 窗口创建配置
- `show: false` - 初始隐藏窗口
- `hardwareAcceleration: true` - 保持GPU加速
- `offscreen: false` - 不使用离屏渲染（使用标准窗口）
- `backgroundThrottling: false` - 禁用后台节流

### 2. 命令行参数
- `--hidden=1/0` - 控制窗口初始状态
- `--delay=毫秒` - 设置窗口创建间隔

### 3. 渲染优化
- `disable-backgrounding-occluded-windows` - 不暂停被遮挡窗口的渲染
- `disable-renderer-backgrounding` - 禁用渲染器后台节流
- `enable-zero-copy` - 启用零拷贝优化

### 4. 大漠兼容性
- 窗口句柄（HWND）保持有效
- 渲染内容持续更新
- GPU渲染上下文不销毁
- 支持DX抓图接口

## 注意事项

### 1. 资源占用
- 隐藏窗口仍然占用GPU资源（比显示窗口少）
- 建议配合 `--delay` 参数使用，避免同时创建过多窗口
- 监控GPU和内存使用情况

### 2. 性能优化建议
- 对于大批量窗口（如15+），建议：
  - 使用 `--hidden=1`
  - 使用 `--delay=1500` 或更长
  - 考虑分批创建窗口

### 3. 大漠调用优化
- 窗口隐藏时，大漠仍然可以正常抓图
- 建议在抓图时隐藏窗口，抓图完成后显示
- 可以通过API动态控制窗口状态

### 4. 兼容性测试
- 确保大漠插件版本支持隐藏窗口抓图
- 测试不同分辨率下的抓图效果
- 验证GPU加速是否正常工作

## 故障排除

### 问题1：隐藏窗口后无法抓图
**解决方法：**
- 确认窗口句柄有效
- 检查GPU加速是否启用
- 尝试先显示窗口再隐藏

### 问题2：资源占用仍然较高
**解决方法：**
- 增加窗口创建延迟 `--delay=2000`
- 减少同时创建的窗口数量
- 检查是否有不必要的渲染进程

### 问题3：窗口显示异常
**解决方法：**
- 重启应用程序
- 检查GPU驱动是否正常
- 尝试禁用透明窗口（设置 `transparent: false`）

## 版本信息

- **优化版本：** 1.1.0
- **适用Electron版本：** 41.3.0+
- **大漠兼容性：** 完全兼容

## 更新日志

### v1.1.0 (2026-05-13)
- 新增窗口隐藏模式支持
- 新增 `/visibility` API接口
- 优化GPU渲染配置
- 改进资源占用情况
- 增强大漠DX抓图兼容性

### v1.0.0 (原始版本)
- 基础VNC客户端功能
- 控制模式和窗口同步
- API控制接口