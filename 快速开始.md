# NoVNC Client OSR离屏渲染方案 - 快速开始

## 首次使用（开发者环境）

### 1. 克隆仓库
```bash
git clone https://github.com/hogan-hong/novnc-cef-client.git
cd novnc-cef-client
```

### 2. 安装依赖
```bash
npm install
```

### 3. 编译Native模块（必须）
```bash
npm run rebuild
```

### 4. 准备配置文件

在同目录下创建 `配置文件.int`，格式如下：

```
组1名称=测试组1
组2名称=测试组2

URL1=vnc://127.0.0.1:5901
窗口标题1=iPhone Se2 D1
控制IP1=192.168.1.101

URL2=vnc://127.0.0.1:5902
窗口标题2=iPhone Se2 D2
控制IP2=192.168.1.102

URL3=vnc://127.0.0.1:5903
窗口标题3=iPhone Se2 D3
控制IP3=192.168.1.103

URL4=vnc://127.0.0.1:5904
窗口标题4=iPhone Se2 D4
控制IP4=192.168.1.104

URL5=vnc://127.0.0.1:5905
窗口标题5=iPhone Se2 D5
控制IP5=192.168.1.105
```

### 5. 启动软件
```bash
npm start
```

或双击 `NoVNC Client.exe`（如果已打包）

### 6. 查看窗口句柄

启动后，在软件同目录下查看 `Log.txt`，找到窗口句柄：

```
[2026/05/13 10:15:25] 窗口 1: 标题="iPhone Se2 D1" HWND=332340
[2026/05/13 10:15:27] 窗口 2: 标题="iPhone Se2 D2" HWND=986480
...
```

### 7. 大漠绑定（易语言）

```易语言
.局部变量 hwnd, 整数型
hwnd = 332340  ' 从Log.txt复制

' DX2抓图（推荐）
大漠.后台_绑定窗口 (hwnd, "dx2", "windows3", "windows", 0)
.局部变量 img, 字节集
img = 大漠.CaptureGIF (0, 0, 200, 200, "test.gif")
```

## 下载使用（普通用户）

### 1. 下载最新版本

访问 GitHub Releases：
```
https://github.com/hogan-hong/novnc-cef-client/releases
```

下载 `NoVNC Client-setup.exe` 或 `NoVNC Client.exe`

### 2. 解压/安装

如果是 `.exe` 文件，直接解压即可（绿色版本）

### 3. 准备配置文件

同目录下创建 `配置文件.int`（见上文格式）

### 4. 启动软件

双击 `NoVNC Client.exe`

### 5. 查看窗口句柄并绑定大漠

同上步骤6-7

## 调试模式

如果需要查看辅助窗口的实际内容：

1. 启动软件后，右下角会出现控制栏
2. 点击"显示"按钮
3. 所有辅助窗口会临时显示
4. 确认画面正常后，点击"隐藏"按钮

## 常见问题

### Q1: 启动后提示"native模块未就绪"
**A**: 必须先编译native模块：
```bash
npm run rebuild
```

### Q2: 大漠抓图黑屏
**A**: 检查以下几点：
1. 确认绑定的是**辅助窗口**的HWND（从Log.txt获取）
2. 确认不是OSR窗口的HWND
3. 确认使用DX2或GDI模式

### Q3: 大漠抓图性能差
**A**:
1. 使用DX2模式（性能最好）
2. 不要修改15fps限帧
3. 抓图范围不要太大

### Q4: 软件占用资源过高
**A**:
1. 检查窗口数量，建议不超过10个
2. 确认native模块已编译
3. 查看任务管理器，确认是正常范围

### Q5: 重新编译native模块

如果native模块编译失败或需要重新编译：

```bash
# Windows
rmdir /s /q build
node-gyp configure
node-gyp build
```

或直接使用：
```bash
npm run rebuild
```

## 技术支持

### 文档
- [OSR离屏渲染优化说明.md](./OSR离屏渲染优化说明.md) - 完整技术文档

### GitHub Issues
https://github.com/hogan-hong/novnc-cef-client/issues

### 作者
Hogan Hong

## 性能指标

| 项目 | 数值 |
|------|------|
| 单窗口CPU占用 | 5-8% |
| 单窗口GPU占用 | 3-5% |
| 单窗口内存占用 | 80-120MB |
| 5窗口总CPU占用 | 20-30% |
| 5窗口总GPU占用 | 12-18% |
| 大漠抓图延迟 | 50-150ms |
| 渲染帧率 | 15fps（固定） |

## 技术特点

✅ **OSR离屏渲染**：无可见窗口，无DWM开销
✅ **15fps限帧**：降低CPU/GPU负载75%
✅ **辅助窗口标准化**：大漠抓图不黑屏
✅ **全程无硬盘IO**：保护SSD，零延迟
✅ **一对一双窗口**：每个OSR对应一个辅助窗口
✅ **GPU加速**：硬件解码和渲染
✅ **固定横屏**：1334×750，适配手游群控

## 版本历史

### v2.0 - OSR离屏渲染方案（当前版本）
- OSR离屏渲染 + 自建辅助窗口
- 15fps限帧，性能提升30-60%
- 大漠抓图不黑屏
- Native Addon GDI绘制

### v1.1 - 透明穿透隐身窗口（已废弃）
- 透明窗口 + 鼠标穿透
- GPU渲染继续运行
- 但大漠抓图黑屏

### v1.0 - 隐藏窗口（已废弃）
- 使用win.hide()隐藏
- 会暂停GPU渲染
- 大漠抓图黑屏