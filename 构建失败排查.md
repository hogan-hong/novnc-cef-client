# 构建问题排查

## 常见问题

### npm install 失败

- 确认Node.js版本为20.x LTS
- 删除 `node_modules` 和 `package-lock.json` 后重试：
  ```bash
  rm -rf node_modules package-lock.json
  npm install
  ```

### electron 下载慢

设置国内镜像：
```bash
npm config set electron_mirror https://npmmirror.com/mirrors/electron/
```

### 打包后运行白屏

- 确认 `配置文件.int` 在exe同目录下
- 检查配置文件编码（支持UTF-8和GBK）
- 查看同目录 `Log.txt` 日志

### VNC连接失败

- 确认目标机器VNC服务和websockify已启动
- 确认端口可访问（防火墙）
- 检查URL格式是否正确
