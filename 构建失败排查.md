# GitHub Actions 构建失败排查

## 📋 如何获取详细错误日志

1. 访问 GitHub Actions 页面
   ```
   https://github.com/hogan-hong/novnc-cef-client/actions
   ```

2. 找到失败的 workflow 运行（红色❌）
   - 点击进入详情页

3. 展开失败的步骤
   - 点击步骤名称展开日志
   - 查看具体的错误信息

4. 复制错误信息
   - 选择错误文本（红色部分）
   - 复制粘贴给我

---

## 🔍 常见失败原因

### 1. npm install 失败

**可能错误：**
```
npm ERR! code E404
npm ERR! 404 Not Found
```

**可能原因：**
- 依赖包下载失败
- 网络问题
- package.json 配置错误

**解决方案：**
- 已添加 `--no-cache` 重试
- 检查 package.json 依赖是否正确

### 2. build:native 失败（预期行为）

**可能错误：**
```
error MSB...
gyp ERR! stack Error: `C:\Program Files (x86)\Microsoft Visual Studio\...
```

**可能原因：**
- Windows runner 没有安装 C++ 构建工具
- binding.gyp 配置问题

**解决方案：**
- ✅ 已设置 `continue-on-error: true`
- ✅ 失败后会继续构建，不影响主应用
- ⚠️ Native 模块功能会被跳过

### 3. build:win 失败

**可能错误：**
```
electron-builder error
Error: Cannot find module 'electron-builder'
```

**可能原因：**
- electron-builder 未安装
- 配置文件错误
- 磁盘空间不足

**解决方案：**
- 已添加构建日志保存到 build.log
- 失败时会显示最后 50 行错误
- 需要检查 package.json build 配置

### 4. dist 目录不存在

**可能错误：**
```
dist directory NOT FOUND
```

**可能原因：**
- electron-builder 没有生成文件
- 配置的输出目录错误
- 构建步骤完全失败

**解决方案：**
- 已添加详细目录检查
- 会列出所有目录，帮助定位问题

---

## 🎯 我已经做的优化

### 最新提交 (5063d8a)

1. **添加详细日志**
   - 每个步骤都有清晰的标题
   - 输出关键信息

2. **错误重试**
   - npm install 失败时会重试（不带缓存）
   - build:native 失败时会继续（不影响主应用）

3. **构建日志保存**
   - 构建日志保存到 build.log
   - 失败时显示最后 50 行错误

4. **详细目录检查**
   - 检查 dist 目录是否存在
   - 列出所有目录和文件
   - 帮助定位问题

---

## 📊 查看新的构建

新构建已触发：
```
https://github.com/hogan-hong/novnc-cef-client/actions
```

查找最新的 workflow 运行（commit: 5063d8a）

---

## 🚀 如果还是失败

### 选项 1：提供错误信息

复制粘贴错误日志的关键部分给我，我会帮你修复。

### 选项 2：本地构建（最可靠）

不需要安装 C++ 工具：

```powershell
git clone https://github.com/hogan-hong/novnc-cef-client.git
cd novnc-cef-client
npm install
npm run build:win
```

**就这么简单！**

- Native 模块会自动跳过（如果有错误）
- 应用仍然会正常构建
- 10-15 分钟搞定

### 选项 3：等待明天

GitHub Actions 服务可能暂时有问题，明天再试可能就好了。

---

## 💡 建议

1. **现在**：查看新的构建日志（5063d8a）
2. **如果失败**：复制错误信息给我
3. **如果 GitHub Actions 持续不行**：本地构建（3 个命令）

---

## 📦 本地构建优势

| 项目 | GitHub Actions | 本地构建 |
|------|---------------|----------|
| 可靠性 | 中等（可能排队） | 高 |
| 速度 | 10-30 分钟（排队） | 10-15 分钟 |
| 调试 | 查看日志 | 直接控制 |
| 依赖 | GitHub 服务 | 只需 Node.js + Git |
| 推荐 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## ❓ 需要帮助？

如果需要帮助，请提供：

1. **哪个步骤失败了？**
   - Setup Node.js
   - Install dependencies
   - Build native addon
   - Build Windows application
   - Check build output

2. **错误信息是什么？**
   - 复制粘贴红色错误文本

3. **或者截图**
   - 失败步骤的日志截图

---

## 📝 Git 提交记录

```
5063d8a - 优化GitHub Actions：添加详细日志和错误重试机制
e898fca - 优化GitHub Actions配置：使用windows-latest和Node 20，简化上传流程
558aca7 - 修复native模块编译：添加gdi32.lib链接库和Windows编译器配置
ab4ad95 - 更新构建指南：添加编译问题修复说明
```

---

## ✅ 下一步

1. 刷新 GitHub Actions 页面
2. 查看新的构建（commit: 5063d8a）
3. 如果失败，提供错误信息
4. 或考虑本地构建（3 个命令搞定）